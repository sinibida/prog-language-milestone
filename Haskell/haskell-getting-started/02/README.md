Basics

- `/=` (`!=`)
- `+`: strict type
  - only number + number

- **succ**
- **max*

- *infix call*

```haskell
10 `div` 3
```

if: `else` is *mandatory*

```haskell
doubleSmallNumber x = if x > 100
                        then x
                        else x * 2
```
